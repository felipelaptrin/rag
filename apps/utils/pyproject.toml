[project]
name = "utils"
version = "0.1.0"
description = "Helper functions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
